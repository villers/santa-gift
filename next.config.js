/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/santa-gift",
    assetPrefix: '/santa-gift',
    images: {unoptimized: true},
};

module.exports = nextConfig;
